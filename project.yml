name: BlackmanClient
targets:
  BlackmanClient:
    type: framework
    platform: iOS
    deploymentTarget: "11.0"
    sources: [BlackmanClient]
    info:
      path: ./Info.plist
      version: 0.0.7
    settings:
      APPLICATION_EXTENSION_API_ONLY: true
    scheme: {}
    dependencies:
    - carthage: AnyCodable
